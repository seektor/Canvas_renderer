import { AbstractCanvasLayer } from "./AbstractCanvasLayer";

export class LayersManager {

    private layers: AbstractCanvasLayer[];

    constructor() {
        this.layers = [];
    }

    // addLayer(Partition layer)
}